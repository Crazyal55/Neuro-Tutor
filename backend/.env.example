# Neuro Tutor Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# ===========================================
# REQUIRED FOR PRODUCTION
# ===========================================

# OpenRouter API Key - Get from https://openrouter.ai/keys
# SECURITY: Never commit this key to version control!
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Database URL - SQLite for development, PostgreSQL for production
# Development: sqlite:///./neuro_tutor.db
# Production: postgresql://user:password@host:port/database
DATABASE_URL=sqlite:///./neuro_tutor.db

# ===========================================
# DEVELOPMENT SETTINGS (safe defaults)
# ===========================================

# Application Debug Mode (false for production, true for development)
DEBUG=False

# CORS Origins - Comma-separated list of allowed frontend URLs
# For development: http://localhost:5173,http://localhost:5180
# For production: https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:5180,http://localhost:3000

# ===========================================
# OPENROUTER CONFIGURATION
# ===========================================

# Default Model - See https://openrouter.ai/models for options
DEFAULT_MODEL=qwen2.5:72b-instruct

# LLM Settings
DEFAULT_TEMPERATURE=0.7
DEFAULT_MAX_TOKENS=1000

# ===========================================
# NOTES
# ===========================================
#
# 1. SECURITY:
#    - Always use environment variables for API keys in production
#    - Never commit .env files to version control
#    - Use different API keys for development vs production
#
# 2. DATABASE:
#    - SQLite is fine for development and small deployments
#    - Consider PostgreSQL for production with multiple users
#    - Database file will be created automatically for SQLite
#
# 3. DOCKER:
#    - These variables are automatically passed to containers
#    - Use docker-compose.yml for local development
#    - Set environment variables in your hosting platform for production
#
